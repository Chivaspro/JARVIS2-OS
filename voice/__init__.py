"""Mark LII voice subsystem (change: modernize-jarvis-architecture).

Realtime speech I/O: Gemini Live audio glue, local TTS/STT engines, the
hybrid Piper pipeline, wake-word detection and audio device selection.
Moved from core/ (task 8.2); `core.*` shims keep old import paths alive.
"""
